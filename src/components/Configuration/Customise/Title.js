import Typography from "@material-ui/core/Typography";
import styled from "styled-components";

const Title = styled(Typography).attrs(() => ({
  variant: "h5",
  style: { margin: "3rem 0px 1.5rem" }
}))``;

export default Title;
